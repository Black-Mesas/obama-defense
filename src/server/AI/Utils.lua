local Utils = {}

function Utils.FindTarget(friendly)
    if friendly then
        -- TODO: Target BossBarOwner or Minion tagged humanoids
        return
    end

    -- Find nearby player
end

return Utils
